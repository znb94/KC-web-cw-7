let person = "نانسي";

function sayHello(name) {
  //   if (name) {
  return `مرحباً, ${name} 👋🏻`;
  //   }
  //   return `مرحباً 👋🏻`;
}

// console.log(sayHello(person));
console.log(sayHello());
